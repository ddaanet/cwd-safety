## Current task

A shell-gotchas scan of the plugin landed five fixes plus a new FR7b, all green under `just precommit`. What needs verifying live in a Claude worktree session, rather than by `just probe`: the restore rewrite now appends a *blank* line before `cd <E>` (FR5a and FR5c, so a trailing `\` continuation cannot swallow it), `_HEREDOC` masks hyphenated and dotted delimiters, and `hooks/hooks.json` quotes `${CLAUDE_PLUGIN_ROOT}`. A worktree session is the venue because it also exercises the FR5a cross-root exclusion and the worktree-aware block messages, which only the real `EnterWorktree` lifecycle drives. FR7b (unset `$CLAUDE_PROJECT_DIR` fails open and shouts instead of blocking with an empty `cd`) has no live path — Claude Code always sets the variable — so the suite is its only coverage.
