## Current task

Cutting the cwd-safety 0.6.0 release. Preflight passed: branch and tree clean, `just precommit` green, and the README audited for this cycle — the restore separator is now described as a blank line, and a Limitations bullet covers the FR7a/FR7b fail-open states that had gone undocumented since 0.3.1. The three commits since `v0.5.0` are the release content and the bump is minor, because `eef2cdb` adds FR7b and `f5df6ba` changes what FR5b matches: `/gitlore:push` to publish memory, then `just release minor`.

Live verification of the `eef2cdb` fixes is finished and must not be repeated. In a real `EnterWorktree` session the blank-line restore, the `_HEREDOC` hyphenated and dotted delimiters, the FR5a cross-root exclusion and the worktree-aware block and warning messages were all confirmed against the working-tree hook; the `hooks.json` quoting fix was confirmed separately with paired nested `claude -p --plugin-dir` runs under a path containing a space. FR7b has no live path and stays suite-only by design.
