## Current task

The design doc is now a hub (`docs/design.md`, 367 lines) plus three `docs/references/` nodes (matchers, worktrees, restore-rewrite), hard-wrapped by rumdl and gated by `just check-docs` (400-line cap, link resolution) inside `just precommit`; CLAUDE.md carries a symbol-to-node routing table. Next is evaluating whether a tree-sitter bash parse should replace the regex matchers (`_EMBEDDED_CD` above all) to remove the documented false positives — this session hit one itself: a heredoc body containing `&& cd` was blocked by FR5b and had to go through the Write tool. Any change there touches decision (j)'s rejected alternative (2) and NFR3's stdlib-only rule, so it is a design decision before it is code.
