## Current task

A shell-gotchas scan of the plugin is next: `scripts/cwd-safety.py` (the shell text it matches, masks and emits — `_mask_opaque`, `_EMBEDDED_CD`, the appended `cd <E>` restore line), `hooks/hooks.json`, and the `justfile` recipes. Just landed: FR5b matches over a quote/comment/heredoc/paren-masked copy of the command and its separator set gained `then`/`do`/`{`; tree-sitter was probed (32/32 correct) and rejected on deployment grounds, recorded under decision (j) in `docs/references/matchers.md`. The restore rewrite was dogfooded live on both cases from the previous handoff (`cd docs && ls -a` with the `ls:*` exclusion preserved, and a trailing heredoc) and held.
