{
  "description": "Guard the agent's Bash working directory. PreToolUse(Bash) blocks drift-inducing `cd` commands and commands issued from a drifted cwd; PostToolUse(Bash) warns after the working directory changes. Both fire the same script, which dispatches on hook_event_name.",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "python3 ${CLAUDE_PLUGIN_ROOT}/scripts/cwd-safety.py",
            "timeout": 5
          }
        ]
      }
    ],
    "PostToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "python3 ${CLAUDE_PLUGIN_ROOT}/scripts/cwd-safety.py",
            "timeout": 5
          }
        ]
      }
    ]
  }
}
